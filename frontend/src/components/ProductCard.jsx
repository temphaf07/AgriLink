import { Link } from 'react-router-dom'; import { MapPin, BadgeCheck } from 'lucide-react';
export default function ProductCard({ product }) { const image = product.images?.[0]?.url; return <Link to={`/products/${product._id}`} className="group panel overflow-hidden transition hover:-translate-y-0.5 hover:shadow-md">{image ? <img src={image} alt={product.name} className="h-44 w-full object-cover"/> : <div className="grid h-44 place-items-center bg-leaf-50 text-5xl">🌾</div>}<div className="p-4"><div className="mb-2 flex items-start justify-between gap-2"><h3 className="font-bold group-hover:text-leaf-700">{product.name}</h3><span className="whitespace-nowrap text-sm font-bold text-leaf-700">₹{product.price}/{product.unit}</span></div><p className="text-sm text-stone-500">{product.category} · {product.availableQuantity} {product.unit} available</p><p className="mt-3 flex items-center gap-1 text-xs text-stone-500"><MapPin size={13}/>{[product.location?.district, product.location?.state].filter(Boolean).join(', ') || 'India'} {product.verificationStatus === 'verified' && <BadgeCheck className="ml-auto text-leaf-600" size={16}/>}</p></div></Link> }
