import { Router } from 'express'; import { createProduct, deleteProduct, getProduct, listProducts, updateProduct, verifyProduct } from '../controllers/productController.js'; import { allowRoles, optionalAuth, protect } from '../middleware/auth.js';
const router = Router(); router.get('/', optionalAuth, listProducts); router.get('/:id', optionalAuth, getProduct); router.use(protect); router.post('/', allowRoles('farmer'), createProduct); router.patch('/:id', allowRoles('farmer'), updateProduct); router.delete('/:id', allowRoles('farmer'), deleteProduct); router.patch('/:id/verification', allowRoles('admin'), verifyProduct); export default router;
