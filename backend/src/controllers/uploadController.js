import { Readable } from 'stream'; import cloudinary, { cloudinaryConfigured } from '../config/cloudinary.js'; import AppError from '../utils/AppError.js'; import asyncHandler from '../utils/asyncHandler.js';
export const uploadImage = asyncHandler(async (req, res) => { if (!req.file) throw new AppError('Please select an image file.', 422); if (!cloudinaryConfigured()) throw new AppError('Cloudinary is not configured. Add credentials to enable uploads.', 503); const result = await new Promise((resolve, reject) => { const stream = cloudinary.uploader.upload_stream({ folder: 'agrilink', resource_type: 'image' }, (error, value) => error ? reject(error) : resolve(value)); Readable.from(req.file.buffer).pipe(stream); }); res.status(201).json({ success: true, image: { url: result.secure_url, publicId: result.public_id } }); });
