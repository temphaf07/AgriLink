import User from '../models/User.js'; import AppError from '../utils/AppError.js'; import asyncHandler from '../utils/asyncHandler.js';
export const updateMe = asyncHandler(async (req, res) => { const allowed = ['name', 'phone', 'address', 'buyerType']; const data = Object.fromEntries(Object.entries(req.body).filter(([key]) => allowed.includes(key))); const user = await User.findByIdAndUpdate(req.user.id, data, { new: true, runValidators: true }); res.json({ success: true, user }); });
export const users = asyncHandler(async (req, res) => { const { role, verificationStatus, page = 1, limit = 20 } = req.query; const filter = {}; if (role) filter.role = role; if (verificationStatus) filter.verificationStatus = verificationStatus; const [data, total] = await Promise.all([User.find(filter).sort('-createdAt').skip((page - 1) * limit).limit(Number(limit)), User.countDocuments(filter)]); res.json({ success: true, data, pagination: { total, page: Number(page), pages: Math.ceil(total / limit) } }); });
export const updateVerification = asyncHandler(async (req, res) => { const { status } = req.body; if (!['pending', 'verified', 'rejected'].includes(status)) throw new AppError('Invalid verification status.', 422); const user = await User.findByIdAndUpdate(req.params.id, { verificationStatus: status }, { new: true }); if (!user) throw new AppError('User not found.', 404); res.json({ success: true, user }); });
